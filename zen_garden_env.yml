name: zen-garden-env-fork
channels:
  - conda-forge
  - gurobi
dependencies:
  - pip
  - python
  - pip:
      - -e .[dev,vis,gurobipy,docs]

# conda env create -f zen_garden_env.yml
# conda remove --name zen-garden-env-fork --all
# -channels --> gurobi
#,gurobipy